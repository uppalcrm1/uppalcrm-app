# Redirect all routes to index.html for Single Page Application (SPA) routing
# This allows React Router to handle client-side routing on Render

# API routes should go to the backend (if any API calls are made to same domain)
/api/* https://uppalcrm-api.onrender.com/api/:splat 200

# Health check endpoint
/health https://uppalcrm-api.onrender.com/health 200

# All other routes should serve index.html to enable client-side routing
/* /index.html 200